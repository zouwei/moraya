# v0.10.0 Mermaid 图表支持

> 迭代版本: v0.10.0
> 上一版本: v0.9.0
> 创建日期: 2026-02-13
> 状态: 📋 计划中

---

## 版本概要

为代码块新增 Mermaid 图表渲染能力。当代码块语言声明为 `mermaid` 时，自动渲染为可视化图表。支持编辑/预览双模式切换，Mermaid 库通过 Vite 动态 import 懒加载，仅在文档包含 Mermaid 代码块时才加载。

---

## 需求背景

### 目标图表类型

| 图表 | Mermaid 标识 | 说明 |
|------|-------------|------|
| 流程图 | `flowchart` / `graph` | Flowchart |
| 时序图 | `sequenceDiagram` | Sequence Diagram |
| 甘特图 | `gantt` | Gantt Chart |
| 状态图 | `stateDiagram-v2` | State Diagram |
| 类图 | `classDiagram` | Class Diagram |
| ER 图 | `erDiagram` | Entity Relationship |
| 饼图 | `pie` | Pie Chart |
| 心智图 | `mindmap` | Mindmap |
| 用户旅程图 | `journey` | User Journey |

### 竞品参考

- **Typora**: 内置 Mermaid 11.3，` ```mermaid ` 代码块自动渲染，右键导出 SVG/PNG
- **GitHub**: 服务端渲染 Mermaid 为 SVG
- **HackMD**: 客户端渲染，预览面板实时更新

---

## 关键决策

### D1: 不使用 `@milkdown/plugin-diagram`

| 对比 | `@milkdown/plugin-diagram` | 自定义插件 |
|------|---------------------------|-----------|
| 版本 | v7.7.0（落后半年） | 适配当前 v7.18.0 |
| Mermaid 依赖 | 硬依赖 `mermaid ^10.9.0` | 可用最新 v11.12.2 |
| 懒加载 | 不支持 | 完全支持 `import()` |
| 体积控制 | 无法优化 | 可精细控制 |

**结论**: 自建插件，复用 v0.9.0 的 CodeBlockView NodeView 基础架构。

### D2: 体积优化策略（Vite 实测数据）

Mermaid v11 完整包 ~2.7 MB minified，对当前 5.25 MB DMG 影响较大。

#### Vite 动态 import + 代码分割

```typescript
// mermaid-renderer.ts — 仅在检测到 mermaid 代码块时加载
const mermaid = (await import('mermaid')).default;
```

Vite 自动将 mermaid 拆分为 50 个独立 chunk（核心 + d3/dagre 共享依赖 + 各图表类型）。Tauri 从本地磁盘加载，延迟 ~100-200ms。

**Vite 构建实测结果**：

| 指标 | 无 Mermaid | 有 Mermaid |
|------|-----------|-----------|
| 总 JS 大小 | 2,388 KB | 4,729 KB (+2,341 KB) |
| chunk 数量 | 27 | 77 (+50) |
| 主 chunk | 1,539 KB | 1,539 KB（不变） |
| `_app/` 目录 | 3.6 MB | 6.0 MB (+2.4 MB) |

**主 chunk 体积不变** — Mermaid 100% 独立于主 bundle。

#### Mermaid 内置二级懒加载

Mermaid v11 的 npm 入口 `mermaid.core.mjs` (44 KB) 本身就是懒加载调度器。它注册所有图表类型的检测器（detector），但每种图表的渲染代码是独立 chunk，仅在检测到对应语法时才 `dynamic import()` 加载：

| 图表类型 | chunk 大小 | 加载时机 |
|----------|-----------|---------|
| flowchart | ~105 KB | 使用时 |
| sequenceDiagram | ~171 KB | 使用时 |
| gantt | ~75 KB | 使用时 |
| erDiagram | ~48 KB | 使用时 |
| stateDiagram | ~20 KB | 使用时 |
| pie | ~8 KB | 使用时 |
| mindmap | ~40 KB | 使用时 |
| journey | ~46 KB | 使用时 |
| 其他 (class, C4, block 等) | ~600 KB 合计 | 使用时 |

**共享依赖**（d3, dagre, cytoscape 等 ~1,200 KB）在首次 `import('mermaid')` 时加载。

#### ~~按需图表类型 API~~（已排除）

经深度调研确认：**Mermaid v11 不存在可靠的用户 API 来选择性加载图表类型**。
- 无 `lazyLoadedDiagrams` 配置选项
- `registerExternalDiagrams()` 用于**新增**图表，无法**减少**内置图表
- `registerDiagram()` 是内部 API，不对外导出
- 缺少 `sideEffects: false` 标记，Vite/Rollup 无法 tree-shake 未使用的图表

但 Mermaid 内部已做好代码分割（见上表），无需手动优化。

#### KaTeX 去重（自动生效）

Mermaid 内部依赖 KaTeX，Moraya 已安装 `katex ^0.16.28`。Vite 去重机制自动复用，实测节省 ~300 KB（预估 2.7 MB → 实测 2.4 MB）。

### D3: 体积预估（基于 Vite 实测）

| 指标 | 数值 |
|------|------|
| 磁盘增量（JS chunks） | **+2.4 MB** |
| 初始加载增量 | **0 KB** |
| 首次渲染加载（core + 共享依赖） | ~1.2 MB |
| 每种图表类型额外加载 | 8 - 171 KB |

**DMG 体积预估**: 5.25 MB → **~7.7 MB**（仍在 10 MB 目标内）。

#### 运行时加载体验（Tauri 本地磁盘）

| 场景 | 加载量 | 预计延迟 |
|------|--------|---------|
| 文档无 Mermaid 代码块 | 0 KB | 0 ms |
| 首次遇到 Mermaid（core + d3） | ~1.2 MB | ~100-200 ms |
| 渲染首个 flowchart | +~105 KB | ~20 ms |
| 渲染第二种图表类型 | +~20-171 KB | ~10-20 ms |

---

## 功能设计

### F1: Mermaid 代码块渲染

#### 编辑/预览双模式

```
预览模式（默认）:
┌──────────────────────────────────────────────────┐
│ [mermaid]                        [✏️ 编辑] [📋]  │  ← 工具栏
├──────────────────────────────────────────────────┤
│                                                    │
│        ┌───────┐     ┌───────┐                    │
│        │ Start │────▶│  End  │                    │  ← SVG 渲染
│        └───────┘     └───────┘                    │
│                                                    │
└──────────────────────────────────────────────────┘

编辑模式（点击编辑按钮）:
┌──────────────────────────────────────────────────┐
│ [mermaid]                        [👁 预览] [📋]  │  ← 工具栏
├──────────────────────────────────────────────────┤
│ graph LR                                          │
│   A[Start] --> B[End]                             │  ← 可编辑源码
│                                                    │
└──────────────────────────────────────────────────┘
```

#### 交互规则

- 文档中存在 ` ```mermaid ` 代码块 → 首次渲染时触发 `import('mermaid')`
- 加载期间显示 loading 动画（旋转图标 + "Loading diagram..."）
- 渲染成功 → 显示 SVG 图表
- 渲染失败（语法错误）→ 显示红色错误提示 + 原始代码
- 编辑模式：代码块可直接编辑 Mermaid 源码
- 切换回预览时自动重新渲染
- 点击 SVG 图表 → 进入编辑模式（便于修改）

### F2: 主题适配

Mermaid 图表需跟随 Moraya 的明/暗主题切换：

```typescript
mermaid.initialize({
  startOnLoad: false,
  theme: isDarkMode ? 'dark' : 'default',
  themeVariables: {
    // 映射 Moraya CSS 变量
    primaryColor: 'var(--accent-color)',
    primaryTextColor: 'var(--text-primary)',
    primaryBorderColor: 'var(--border-color)',
    lineColor: 'var(--text-secondary)',
    secondaryColor: 'var(--bg-secondary)',
    tertiaryColor: 'var(--bg-hover)',
  },
});
```

主题切换时重新调用 `mermaid.initialize()` 并重新渲染所有 Mermaid 代码块。

### F3: HTML 导出

导出 HTML 时，Mermaid 代码块需渲染为内联 SVG：

- 导出前调用 `mermaid.render()` 将每个 Mermaid 代码块转换为 SVG
- SVG 内联到 HTML 中（无需外部 JS）
- 渲染失败的代码块保留为 `<pre><code>` 原始代码

---

## 技术方案

### 架构

```
src/lib/editor/plugins/
├── code-block-view.ts           # v0.9.0 已有：CodeBlock NodeView
└── mermaid-renderer.ts          # 新增：Mermaid 渲染器（懒加载封装）

src/lib/editor/setup.ts          # 修改：mermaid 渲染注入到 codeBlockView
src/lib/styles/editor.css        # 修改：Mermaid 预览/加载/错误样式
src/lib/services/export-service.ts # 修改：导出时渲染 Mermaid 为 SVG
```

### Mermaid 渲染器封装

```typescript
// mermaid-renderer.ts
let mermaidModule: typeof import('mermaid') | null = null;
let loadingPromise: Promise<void> | null = null;

export async function ensureMermaidLoaded(): Promise<void> {
  if (mermaidModule) return;
  if (loadingPromise) return loadingPromise;

  loadingPromise = (async () => {
    const mod = await import('mermaid');
    mermaidModule = mod;
    mod.default.initialize({
      startOnLoad: false,
      theme: getCurrentTheme() === 'dark' ? 'dark' : 'default',
    });
  })();

  return loadingPromise;
}

export async function renderMermaid(
  code: string,
  id: string
): Promise<{ svg: string } | { error: string }> {
  await ensureMermaidLoaded();
  try {
    const { svg } = await mermaidModule!.default.render(id, code);
    return { svg };
  } catch (e) {
    return { error: e instanceof Error ? e.message : 'Render failed' };
  }
}
```

### CodeBlockView 集成

在 v0.9.0 的 `code-block-view.ts` 基础上，当 `language === 'mermaid'` 时切换为 Mermaid 专用渲染：

```typescript
// code-block-view.ts — update() 方法
if (updatedNode.attrs.language === 'mermaid') {
  // 隐藏代码 contentDOM，显示 SVG 预览容器
  showMermaidPreview(updatedNode.textContent, previewContainer);
  return true;
}
```

### CSP 兼容性

Mermaid 渲染生成 SVG 内联 `<style>`。需要确保 CSP `style-src` 包含 `'unsafe-inline'`。

当前 `tauri.conf.json` CSP:
```
"csp": "default-src 'self'; script-src 'self'; style-src 'self' 'unsafe-inline'; connect-src ipc: http://ipc.localhost; img-src 'self' asset: http://asset.localhost data: blob:"
```

`style-src 'unsafe-inline'` 已包含，Mermaid SVG 内联样式可正常工作。

---

## 修改文件清单

| 文件 | 操作 | 说明 |
|------|------|------|
| `src/lib/editor/plugins/mermaid-renderer.ts` | 新增 | Mermaid 懒加载封装 + 渲染 API |
| `src/lib/editor/plugins/code-block-view.ts` | 修改 | Mermaid 代码块检测 + 预览/编辑双模式 |
| `src/lib/editor/setup.ts` | 修改 | Tier1 新增 mermaid 渲染器 |
| `src/lib/styles/editor.css` | 修改 | Mermaid 预览/loading/错误样式 |
| `src/lib/services/export-service.ts` | 修改 | 导出时渲染 Mermaid 为内联 SVG |
| `src/lib/i18n/locales/en.json` | 修改 | `mermaid.loading`, `mermaid.error`, `mermaid.edit`, `mermaid.preview` |
| `src/lib/i18n/locales/zh-CN.json` | 修改 | 对应中文翻译 |
| `package.json` | 修改 | 新增 `mermaid: ^11.12.2` 依赖 |

## 新增依赖

| 包名 | 版本 | 类型 | 大小（Vite 实测） |
|------|------|------|-----------------|
| `mermaid` | ^11.12.2 | dependencies | +2.4 MB（50 个懒加载 chunk，主 chunk 不变） |

> KaTeX 去重已生效（预估 2.7 MB → 实测 2.4 MB）。初始加载零增量。

---

## 验证方案

1. `pnpm check` — 0 TypeScript 错误
2. `cargo check` — Rust 编译通过
3. 新建 ` ```mermaid ` 代码块，输入流程图代码 → SVG 图表正确渲染
4. 文档无 Mermaid 代码块 → `mermaid` chunk 未加载（检查 Network/控制台）
5. 点击编辑按钮 → 切换为代码编辑模式，可修改 Mermaid 源码
6. 切换回预览 → 图表重新渲染
7. Mermaid 语法错误 → 显示错误提示，不崩溃
8. 暗色主题 → 图表配色正确切换
9. 导出 HTML → Mermaid 图表渲染为内联 SVG
10. 测试全部 9 种图表类型（flowchart, sequenceDiagram, gantt, stateDiagram-v2, classDiagram, erDiagram, pie, mindmap, journey）
11. 构建后检查 DMG 体积变化
12. 验证初始加载性能不退化（mermaid chunk 未在启动时加载）

---

## 实施优先级

1. **P0**: mermaid-renderer.ts + code-block-view.ts Mermaid 检测 + 渲染
2. **P1**: 编辑/预览双模式切换
3. **P1**: 主题适配（明/暗切换）
4. **P2**: HTML 导出 Mermaid → SVG
5. **P2**: 体积优化（验证按需图表类型加载）
