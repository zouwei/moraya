# 005 — Phase 5: 创作发布工作流 (v0.2.0)

> 迭代版本: v0.2.0
> 创建日期: 2026-02-05
> 状态: 已完成

## 迭代目标

为 Moraya 编辑器新增**创作→优化→发布**完整工作流，形成从写作到发布的闭环。用户写完文章后，可一键完成 SEO 优化、AI 配图、多目标发布，区别于其他 Markdown 编辑器的核心差异化能力。

## 功能清单

| 编号 | 功能 | 优先级 | 依赖 | 状态 |
|------|------|--------|------|------|
| F5.1 | SEO 优化助手 | P0 | 已有文本 AI | ✅ |
| F5.2 | AIGC 图片生成配置 | P0 | 无 | ✅ |
| F5.3 | AI 配图工作流 | P0 | F5.1 + F5.2 | ✅ |
| F5.4 | 发布目标管理 | P1 | 无 | ✅ |
| F5.5 | 发布工作流 UI | P1 | F5.1 + F5.3 + F5.4 | ✅ |
| F5.6 | 图片工具系统 | P1 | 无 | ✅ |
| F5.7 | 图床服务 | P1 | 无 | ✅ |

---

## F5.1 SEO 优化助手

### 需求描述

AI 分析文章内容（截取前 8000 字符），自动生成发布所需的 SEO 元数据。支持中英文双语。

### 输出数据结构

```typescript
interface SEOData {
  titles: string[];           // 3 个 SEO 优化候选标题（每个 <70 字符）
  selectedTitle: string;      // 用户选中的标题（默认第一个）
  excerpt: string;            // 摘要（≤120 字符）
  tags: string[];             // 5-8 个关键词标签
  metaDescription: string;    // meta description（≤160 字符）
  slug: string;               // URL 友好的英文短路径
}
```

### AI Prompt 行为

- 生成 3 个候选标题（保留原文核心含义，SEO 友好）
- 中文文章生成中文标题，但 slug 始终为英文
- 强制 JSON 响应格式（无 markdown 包裹）

### 数据存储

- **存储位置**: Tauri plugin-store，key 为 `seo:{filePath}`
- **零文件污染**: 不在用户工作目录生成任何文件
- **发布时合并**: 导出/发布时自动拼装为 front matter

### UI 交互

- **入口**: 创作工作流面板中的步骤项
- **面板形式**: 底部抽屉（Bottom Sheet），从 StatusBar 上方弹出
- **布局**: 编辑区上半部分仍可见，用户可对照修改

### 底部抽屉内容

1. **标题候选** — radio 单选 + 自定义输入框
2. **摘要** — 可编辑 textarea，右下角字数计数器
3. **标签** — 药丸样式 chips，可删除/添加
4. **Slug** — 单行输入框，AI 自动生成
5. **操作栏** — `↻ 重新生成` | `应用到文章`

### 文件清单

| 文件 | 类型 | 说明 |
|------|------|------|
| `src/lib/components/SEOPanel.svelte` | 新建 | 底部抽屉面板 UI |
| `src/lib/services/ai/seo-service.ts` | 新建 | SEO 分析逻辑（调用已有 AI service） |
| `src/lib/services/ai/types.ts` | 修改 | 新增 SEOData 接口 |

---

## F5.2 AIGC 图片生成配置

### 需求描述

在设置面板 AI 标签页中新增「图片生成 AI」配置区块。支持比例 + 分辨率档位的灵活尺寸系统。

### 支持的 Provider

| Provider | Base URL | 默认模型 | 说明 |
|----------|----------|----------|------|
| OpenAI DALL-E | `https://api.openai.com/v1` | `dall-e-3` | 标准实现 |
| Grok (xAI) | `https://api.x.ai/v1` | `grok-2-image` | OpenAI 兼容格式 |
| Custom | 用户自填 | 用户自填 | 适配阿里云通义万相等（自动识别 DashScope） |

### 统一接口

三个 Provider 共享 OpenAI-compatible 协议（`/images/generations`），差异仅在预填配置值。当检测到 DashScope URL 时，自动切换为阿里云原生多模态 API。

```typescript
type ImageProvider = 'openai' | 'grok' | 'custom';
type ImageAspectRatio = '16:9' | '4:3' | '3:2' | '1:1' | '2:3' | '3:4' | '9:16';
type ImageSizeLevel = 'large' | 'medium' | 'small';

interface ImageProviderConfig {
  provider: ImageProvider;
  baseURL: string;
  apiKey: string;
  model: string;
  defaultRatio: ImageAspectRatio;       // 默认 '16:9'
  defaultSizeLevel: ImageSizeLevel;     // 默认 'medium'
}
```

### 图片尺寸矩阵

7 种比例 × 3 种分辨率档位 = 21 种预设尺寸：

| 比例 | Large | Medium | Small |
|------|-------|--------|-------|
| 16:9 | 1792×1008 | 1280×720 | 1024×576 |
| 4:3 | 1536×1152 | 1280×960 | 1024×768 |
| 3:2 | 1536×1024 | 1280×854 | 1024×682 |
| 1:1 | 1536×1536 | 1024×1024 | 768×768 |
| 2:3 | 1024×1536 | 854×1280 | 682×1024 |
| 3:4 | 1152×1536 | 960×1280 | 768×1024 |
| 9:16 | 1008×1792 | 720×1280 | 576×1024 |

### DashScope 适配

- 自动检测 baseURL 中的 `dashscope` 关键字
- 切换为原生 `/api/v1/services/aigc/multimodal-generation/generation` API
- 尺寸格式从 `1024x1024` 转换为 `1024*1024`
- 使用 Tauri HTTP 插件绕过 CORS

### UI 布局

在 AI 标签页内部，对话 AI 区块下方新增「图片生成 AI」区块（分割线分隔）：

- Provider 下拉选择（切换时自动填充 baseURL/model 预设值）
- API Key 输入框
- Model 输入框
- 默认比例下拉选择（7 种比例）
- 分辨率档位选择（large / medium / small）+ 实时分辨率预览

### 文件清单

| 文件 | 类型 | 说明 |
|------|------|------|
| `src/lib/components/ai/AISettings.svelte` | 修改 | 新增图片生成 AI 配置区块 |
| `src/lib/services/ai/types.ts` | 修改 | 新增 ImageProviderConfig、IMAGE_SIZE_MAP |
| `src/lib/services/ai/image-service.ts` | 新建 | 图片生成 API 调用（含 DashScope 适配器） |
| `src/lib/stores/settings-store.ts` | 修改 | 新增 imageProviderConfig 配置存储 |

---

## F5.3 AI 配图工作流

### 需求描述

基于文章内容，AI 生成图片提示词，再调用 AIGC API 生成配图，插入文章。支持 5 种生图模式，每种模式 10 种专业图片风格。

### 五种生图模式

| 模式 | 说明 | 典型场景 |
|------|------|----------|
| Article（文章配图） | 图文并茂的文章插图 | 博客、技术文档 |
| Design（设计可视化） | 多角度产品/工业设计渲染 | 产品展示、概念设计 |
| Storyboard（故事板） | 按时间线的叙事场景帧 | 脚本、教程、流程说明 |
| Product（产品摄影） | 商业级产品照片 | 电商、产品页 |
| Moodboard（情绪板） | 抽象/氛围类灵感图 | 品牌、创意方案 |

### 模式专属图片风格（每种 10 个）

```typescript
type ImageGenMode = 'article' | 'design' | 'storyboard' | 'product' | 'moodboard';

const MODE_STYLES: Record<ImageGenMode, ImageStyle[]> = {
  article:    ['auto', 'photo', 'illustration', 'flat', 'ink', 'watercolor', 'isometric', 'infographic', 'editorial', 'cartoon'],
  design:     ['auto', 'render', 'sketch', 'blueprint', 'clay', 'wireframe', 'exploded', 'section', 'cad', 'prototype'],
  storyboard: ['auto', 'anime', 'comic', 'cinematic', 'watercolor', 'pixel', 'noir', 'manga', 'cartoon', 'realistic'],
  product:    ['auto', 'studio', 'lifestyle', 'flatlay', 'macro', 'minimalist', 'packaging', 'outdoor', 'editorial', 'mood'],
  moodboard:  ['auto', 'abstract', 'texture', 'gradient', 'collage', 'vintage', 'botanical', 'geometric', 'ethereal', 'brutalist'],
};
```

- 每种模式的第一个风格统一为 `auto`（AI 自动选择最合适的风格）
- 切换模式时自动重置风格为 `auto`

### 三步流程

#### Step 1: 生成提示词

- 文本 AI 分析文章 → 输出 N 条图片提示词（JSON 格式）
- 每条提示词包含：`prompt`（英文）、`target`（目标段落索引）、`reason`（对应段落说明）
- 可配置：
  - **生图模式**（5 种模式切换，药丸按钮）
  - **图片风格**（下拉框，随模式动态切换可选项）
  - **图片比例**（7 种比例下拉）
  - **分辨率档位**（large/medium/small + 实时分辨率显示）
  - **图片张数**（1-5，下拉选择）
- 提示词可编辑、可删除、可添加、可重新生成
- 布局：第一行（模式标题 + 药丸按钮 + 风格下拉），第二行（比例 + 分辨率 + 张数）

#### Step 2: 生成图片

- 调用 AIGC API 逐张生成，显示 loading 状态
- **图片预览比例自适应**：根据 Step 1 选择的比例自动调整预览框的 aspect-ratio
- 每张图下方：`↻` 重新生成 | `✓` 选中/取消
- Grid 布局：`repeat(auto-fill, minmax(200px, 1fr))`
- 底部显示已选数量

#### Step 3: 插入文章

- **插入位置**（默认第一项）:
  1. 每张图插入对应段落后（推荐）
  2. 文章末尾
  3. 仅复制 Markdown 到剪贴板
- **图片来源**: 直接使用 AI 返回的 URL
- 确认后插入 `<img>` 标签到编辑器，浮层关闭

### AI System Prompts（按模式区分）

每种模式有独立的 system prompt，引导 AI 生成符合该模式特征的图片提示词：

- **Article**: 关注概念场景、隐喻、增强读者理解
- **Design**: 多角度产品渲染，清晰的材质纹理和表面工艺
- **Storyboard**: 时间线叙事，角色姿态、镜头语言、场景衔接一致性
- **Product**: 商业摄影手法，柔光、干净背景、道具搭配
- **Moodboard**: 抽象氛围，色彩调性、纹理、几何构成

### UI 交互

- **入口**: 创作工作流面板中的步骤项
- **面板形式**: 居中浮层（Overlay Dialog），640×520px
- **步骤指示器**: `Step 1 ● ── Step 2 ○ ── Step 3 ○`

### 文件清单

| 文件 | 类型 | 说明 |
|------|------|------|
| `src/lib/components/ImageGenDialog.svelte` | 新建 | 配图三步向导浮层（含模式/风格/比例/张数控制） |
| `src/lib/services/ai/image-service.ts` | 修改 | 5 种模式 system prompt + MODE_STYLES + 提示词生成 + 图片生成调用 |
| `src/lib/services/ai/types.ts` | 修改 | 新增 ImagePrompt、ImageAspectRatio、ImageSizeLevel |

---

## F5.4 发布目标管理

### 需求描述

Settings 中新增 Publish 标签页，支持添加**多个**发布目标（多个 GitHub 仓库 + 多个自定义 API）。

### 目标类型

#### GitHub 仓库

```typescript
interface GitHubTarget {
  type: 'github';
  id: string;
  name: string;
  repoUrl: string;             // https://github.com/user/blog
  branch: string;              // main
  articlesDir: string;         // content/posts/
  imagesDir: string;           // static/images/
  token: string;               // GitHub PAT
  frontMatterPreset: string;   // 'hugo' | 'hexo' | 'astro' | 'custom'
  frontMatterTemplate: string; // 模板字符串（支持 {{变量}}）
  fileNamePattern: string;     // 文件命名规则，如 '{{date}}-{{slug}}'
}
```

#### Custom API

```typescript
interface CustomAPITarget {
  type: 'custom-api';
  id: string;
  name: string;
  endpoint: string;            // https://my-server.com/api/articles
  method: 'POST' | 'PUT';
  headers: Record<string, string>;
  bodyTemplate: string;        // JSON 模板（支持 {{变量}}）
  fileNamePattern: string;     // 文件命名规则
}
```

### 模板变量

```
{{title}}        — SEO 标题（或文章一级标题）
{{date}}         — 发布日期 YYYY-MM-DD
{{tags}}         — 标签（逗号分隔）
{{description}}  — meta description
{{slug}}         — URL slug
{{cover}}        — 封面图路径/URL
{{excerpt}}      — 摘要
{{content}}      — 文章正文（Markdown）
```

### Front Matter 预设模板

提供 Hugo / Hexo / Astro 三个预设 + 自定义，用户可在下拉框快速切换，也可直接编辑模板内容。

### 文件命名规则

支持自定义文件命名模式，方便在目标目录中按时间线排序。

**4 种预设**：

| 预设 | 模式 | 输出示例 |
|------|------|----------|
| 日期 + Slug（默认） | `{{date}}-{{slug}}` | `2026-02-05-my-article.md` |
| 简单（原文件名） | `{{filename}}` | `my-article.md` |
| 日期 + 文件名 | `{{date}}-{{filename}}` | `2026-02-05-my-article.md` |
| 年/月/Slug 目录 | `{{year}}/{{month}}/{{slug}}` | `2026/02/my-article.md` |

**可用变量**：`{{date}}`, `{{year}}`, `{{month}}`, `{{day}}`, `{{slug}}`, `{{filename}}`, `{{title}}`

**规则**：
- 扩展名固定 `.md`
- `{{slug}}` 为空时自动 fallback 到 `{{filename}}`，反之亦然
- 路径中的特殊字符自动净化（`<>:"|?*` → `-`）
- 支持 `/` 创建子目录（如 `{{year}}/{{month}}/{{slug}}` 在 articlesDir 下创建嵌套目录）

**UI**：预设下拉框 + 可编辑 input + 实时预览，适用于所有发布目标类型。

### UI 布局

- Settings 新增 `Publish` 标签页（图标 📮）
- 发布目标列表：卡片式展示，每张卡片显示名称、类型（🔵 GitHub / 🟣 Custom API）
- 操作：`+ 添加` | `编辑` | `删除` | `测试连接`

### 技术实现

- GitHub 发布使用 **GitHub Contents API**（`PUT /repos/{owner}/{repo}/contents/{path}`），无需 git clone，轻量无依赖
- Custom API 使用 `fetch()` 直接 POST/PUT
- 认证方式首期仅支持 Personal Access Token
- 文件路径由 `articlesDir + resolveFileName(pattern, variables)` 组合

### 文件清单

| 文件 | 类型 | 说明 |
|------|------|------|
| `src/lib/components/PublishSettings.svelte` | 新建 | 发布目标配置面板（含文件命名规则） |
| `src/lib/services/publish/types.ts` | 新建 | 发布目标类型定义 + FILE_NAME_PRESETS + resolveFileName |
| `src/lib/services/publish/github-publisher.ts` | 新建 | GitHub Contents API 发布逻辑 |
| `src/lib/services/publish/api-publisher.ts` | 新建 | Custom API 发布逻辑 |
| `src/lib/services/publish/index.ts` | 新建 | 模块导出 |
| `src/lib/stores/settings-store.ts` | 修改 | 新增 publishTargets 配置 |
| `src/lib/components/SettingsPanel.svelte` | 修改 | 新增 Publish 标签页 |

---

## F5.5 发布工作流 UI

### 需求描述

StatusBar 新增「创作工作流」按钮，点击弹出上拉面板，聚合 SEO → 配图 → 发布三个步骤。

### StatusBar 变化

```
┌──────────────────────────────────────────────────────────┐
│  512 字 · 2,847 字符  │  可视化  │  源码  │  分屏  │  📤 创作工作流  │
└──────────────────────────────────────────────────────────┘
```

StatusBar 中央为三态模式切换按钮（Visual / Source / Split），右侧新增「创作工作流」按钮。

### 上拉面板

从 StatusBar 上方弹出（fixed 定位，300px 宽，右下角对齐），slide-up 动画：

```
┌─────────────────────────────┐
│  创作工作流                  │
│                             │
│  ☐  ✦ SEO 优化              │
│     生成标题/摘要/标签        │
│                             │
│  ☐  🎨 AI 配图              │
│     生成文章配图              │
│                             │
│  ── 以上步骤可选 ──          │
│                             │
│  ○  📤 发布                  │
│     推送到目标平台            │
└─────────────────────────────┘
```

### 步骤状态

| 状态 | 图标 | 说明 |
|------|------|------|
| 未执行 | ☐ | 可点击触发 |
| 已完成 | ☑ | 带完成标记 |

### 交互流程

1. 点击「✦ SEO」→ 工作流面板关闭 → 弹出底部抽屉 → 完成后状态变 ☑
2. 点击「🎨 配图」→ 工作流面板关闭 → 弹出居中浮层 → 完成后状态变 ☑
3. 点击「📤 发布」→ 工作流面板关闭 → 弹出目标选择（支持**多选**同时发布到多个目标）→ 确认发布
4. SEO 和配图**可选可跳过**，用户可以直接点发布
5. 切换文档时自动重置工作流状态

### 文件清单

| 文件 | 类型 | 说明 |
|------|------|------|
| `src/lib/components/PublishWorkflow.svelte` | 新建 | 上拉工作流面板 |
| `src/lib/components/PublishConfirm.svelte` | 新建 | 发布确认浮层（目标选择 + 发布执行） |
| `src/lib/components/StatusBar.svelte` | 修改 | 新增创作工作流按钮 |
| `src/routes/+page.svelte` | 修改 | 工作流状态管理（showWorkflow / showSEOPanel / showImageGenDialog） |

---

## F5.6 图片工具系统

### 需求描述

图片插入后支持两种交互：**单击**显示浮动缩放工具条，**右键**显示完整上下文菜单。通用能力，不仅服务于 AI 配图，用户手动插入的图片也可使用。

### 浮动缩放工具条（单击图片）

- 图片上方出现水平工具条
- 预设：`[25%] [50%] [75%] [100%] [原始大小]`
- 当前尺寸高亮（accent 色）
- 点击其他区域 → 工具条消失

### 右键上下文菜单

- 菜单项：
  - **调整大小** → 子菜单（25% / 50% / 75% / 100% / 原始大小）
  - **上传到图床**（仅本地/blob 图片显示）
  - **编辑描述**
  - **复制图片地址**
  - ──分隔线──
  - **删除图片**（红色危险操作）

### 技术实现

- 在 Editor.svelte 中通过 ProseMirror 事件监听处理图片交互
- 图片 `width` 通过内联 HTML `<img src="..." width="75%">` 格式输出到 Markdown
- 缩放工具条和上下文菜单均为独立 Svelte 组件，通过 fixed 定位显示

### 文件清单

| 文件 | 类型 | 说明 |
|------|------|------|
| `src/lib/editor/ImageToolbar.svelte` | 新建 | 浮动缩放工具条 UI |
| `src/lib/editor/ImageContextMenu.svelte` | 新建 | 右键上下文菜单（含子菜单） |
| `src/lib/editor/Editor.svelte` | 修改 | 图片交互事件处理（单击/右键） |
| `src/lib/styles/editor.css` | 修改 | 图片缩放相关样式 |

---

## F5.7 图床服务

### 需求描述

Settings 中新增「图片」标签页，支持配置图床服务。用户可将文章中的图片上传到图床获取永久 URL。

### 支持的图床

| Provider | API 地址 | 认证方式 |
|----------|----------|----------|
| SM.MS | `https://sm.ms/api/v2/upload` | Bearer Token |
| Imgur | `https://api.imgur.com/3/image` | Client-ID Header |
| Custom | 用户自填 | Bearer Token + 自定义 Headers |

### 配置数据结构

```typescript
type ImageHostProvider = 'smms' | 'imgur' | 'custom';

interface ImageHostConfig {
  provider: ImageHostProvider;
  apiToken: string;
  customEndpoint: string;
  customHeaders: string;        // JSON 字符串
  autoUpload: boolean;          // 粘贴/拖入图片时自动上传
}
```

### 功能

- **手动上传**：通过图片右键菜单「上传到图床」触发
- **自动上传**：开启 autoUpload 后，粘贴/拖入图片时自动上传
- **测试上传**：生成 1×1 红色像素 PNG 测试连接
- **状态反馈**：idle / testing / success / failed（3 秒后重置）

### 文件清单

| 文件 | 类型 | 说明 |
|------|------|------|
| `src/lib/components/ImageHostingSettings.svelte` | 新建 | 图床配置面板 |
| `src/lib/services/image-hosting/types.ts` | 新建 | 图床类型定义 |
| `src/lib/services/image-hosting/upload.ts` | 新建 | 上传逻辑（SM.MS / Imgur / Custom） |
| `src/lib/services/image-hosting/index.ts` | 新建 | 模块导出 |
| `src/lib/stores/settings-store.ts` | 修改 | 新增 imageHostConfig 配置存储 |
| `src/lib/components/SettingsPanel.svelte` | 修改 | 新增「图片」标签页 |

---

## 附加增强

### AI 面板可拖拽宽度

- AI 聊天面板左侧边缘可拖拽调整宽度
- 范围：280px ~ 800px，默认 340px
- 拖拽时显示 accent 色高亮条

### 文件

| 文件 | 类型 | 说明 |
|------|------|------|
| `src/lib/components/ai/AIChatPanel.svelte` | 修改 | 新增 resize-handle + 拖拽逻辑 |

---

## Settings 标签页总览

Phase 5 完成后，Settings 面板包含以下标签页：

| 标签 | 图标 | 内容 |
|------|------|------|
| 通用 | ⚙ | 语言、自动保存 |
| 编辑器 | ✎ | 行宽、制表符、行号 |
| 外观 | ◐ | 主题、深色模式、字体 |
| AI | ✦ | 对话 AI 配置 + 图片生成 AI 配置 |
| MCP | ⇌ | MCP 服务器管理 |
| 图片 | ▣ | 图床服务配置 |
| 发布 | 📮 | 发布目标管理 |

---

## 新增文件总览

| 文件路径 | 功能 |
|----------|------|
| `src/lib/components/SEOPanel.svelte` | SEO 底部抽屉 |
| `src/lib/components/ImageGenDialog.svelte` | 配图三步向导（5 模式 × 10 风格） |
| `src/lib/components/PublishWorkflow.svelte` | 创作工作流面板 |
| `src/lib/components/PublishConfirm.svelte` | 发布确认浮层 |
| `src/lib/components/PublishSettings.svelte` | 发布目标配置（含文件命名规则） |
| `src/lib/components/ImageHostingSettings.svelte` | 图床服务配置 |
| `src/lib/editor/ImageToolbar.svelte` | 浮动缩放工具条 |
| `src/lib/editor/ImageContextMenu.svelte` | 图片右键上下文菜单 |
| `src/lib/services/ai/seo-service.ts` | SEO 分析服务 |
| `src/lib/services/ai/image-service.ts` | 图片生成服务（含 DashScope 适配） |
| `src/lib/services/publish/types.ts` | 发布类型定义 + 文件命名 |
| `src/lib/services/publish/github-publisher.ts` | GitHub Contents API 发布 |
| `src/lib/services/publish/api-publisher.ts` | Custom API 发布 |
| `src/lib/services/publish/index.ts` | 发布模块导出 |
| `src/lib/services/image-hosting/types.ts` | 图床类型定义 |
| `src/lib/services/image-hosting/upload.ts` | 图床上传逻辑 |
| `src/lib/services/image-hosting/index.ts` | 图床模块导出 |

## 修改文件总览

| 文件路径 | 修改内容 |
|----------|----------|
| `src/lib/services/ai/types.ts` | SEOData、ImageProviderConfig、IMAGE_SIZE_MAP |
| `src/lib/stores/settings-store.ts` | imageProviderConfig、imageHostConfig、publishTargets |
| `src/lib/components/ai/AISettings.svelte` | 图片生成 AI 配置区块 |
| `src/lib/components/ai/AIChatPanel.svelte` | 可拖拽宽度 |
| `src/lib/components/StatusBar.svelte` | 创作工作流按钮 |
| `src/lib/components/SettingsPanel.svelte` | 新增「图片」和「发布」标签页 |
| `src/lib/editor/Editor.svelte` | 图片交互（单击工具条 + 右键菜单） |
| `src/lib/styles/editor.css` | 图片缩放样式 |
| `src/lib/i18n/locales/zh-CN.json` | 全部新增 i18n key |
| `src/lib/i18n/locales/en.json` | 全部新增 i18n key |
| `src/routes/+page.svelte` | 工作流状态管理 + 组件集成 |

---

## 验证方案

1. `pnpm check` — 零 TypeScript 错误
2. `pnpm build` — 前端构建成功
3. 功能验证：
   - SEO：配置 AI → 写文章 → 触发 SEO → 确认元数据正确生成并存入 store
   - 配图：配置 AIGC → 选择模式/风格/比例 → 生成提示词 → 生成图片 → 验证预览比例自适应 → 插入文章
   - 发布：配置 GitHub 目标（含文件命名规则）→ 发布 → 确认仓库中文件路径正确
   - 工作流：StatusBar 按钮 → 步骤面板 → SEO ☑ → 配图 ☑ → 发布 → 完成
   - 图片缩放：插入图片 → 单击出现工具条 → 选择百分比 → 右键出现菜单 → 验证全部操作
   - 图床：配置图床 → 测试上传 → 右键图片上传到图床 → 验证 URL 替换
   - 文件命名：配置不同命名规则 → 预览正确 → 发布后确认文件路径
